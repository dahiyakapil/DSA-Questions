#include <bits/stdc++.h>
using namespace std;

int main()
{
    
    //    string str;
    //    cin >> str;
    //    cout << str;
    //    cout << endl;

        //To  print the complete line of string then getline(cin, str) function is used

        // string str2;
        // getline(cin, str2);
        // cout << str2;
        // cout<<endl;

        /* 
        Inbuilt function of string
        reverse()
        substr()
        + operator
        push_back()
        size()
        length(
        to_String
        )

        */

        // string s;
        // getline(cin, s);
        // cout << s <<endl;

        // s.push_back('k');
        // cout << s << endl;

        // s.pop_back();
        // cout << s <<endl;

        // reverse(s.begin(), s.end());
        // cout << s  << endl;

        // s.substr(0, 2);
        // cout << s << endl;

        // string s2;
        // getline(cin, s2);

        // string s3 = s + s2;
        // cout << s3;

        // s2.size();

        int num = 432;
        string str = to_string(num);
        str += '1';
        cout << str;


    return 0;
}